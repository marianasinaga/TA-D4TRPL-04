import { ExtensionType } from '../provider';
interface RegisterClientParams {
    logMessages?: boolean;
    logWarnings?: boolean;
    extension?: ExtensionType;
}
export declare function registerClient({ logMessages, logWarnings, extension }: RegisterClientParams): void;
export declare function call<RESULT>(method: string, ...params: any[]): Promise<RESULT>;
export {};
